[package]
name = "backup"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
diff = { path = "../diff" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }
dirs = "6"
